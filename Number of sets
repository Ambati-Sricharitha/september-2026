import math

class Solution:
    def numberOfSets(self, n: int, k: int) -> int:
        MOD = 10**9 + 7
        # Using math.comb to find (n + k - 1) choose (2 * k)
        return math.comb(n + k - 1, 2 * k) % MOD
